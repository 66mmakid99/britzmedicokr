---
interface Props {
  items: { title: string; href: string; description?: string }[];
  title?: string;
}

const { items, title = '관련 콘텐츠' } = Astro.props;
---

{items.length > 0 && (
  <section class="my-12">
    <h2 class="mb-6 text-2xl font-bold text-neutral-900">{title}</h2>
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {items.map((item) => (
        <a href={item.href} class="group rounded-2xl border border-neutral-200 bg-white p-5 transition-all duration-300 hover:-translate-y-0.5 hover:shadow-md">
          <p class="font-semibold text-neutral-900 group-hover:text-[#B8960C]">{item.title}</p>
          {item.description && (
            <p class="mt-2 text-sm text-neutral-500">{item.description}</p>
          )}
        </a>
      ))}
    </div>
  </section>
)}
