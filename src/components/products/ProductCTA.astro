---
interface Props {
  productName: string;
  headline?: string;
  description?: string;
}
const { productName, headline, description } = Astro.props;
---

<section class="bg-zinc-950">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
    <h2 class="text-2xl lg:text-3xl font-bold text-white mb-3">
      {headline || `${productName} 도입 상담을 받아보세요`}
    </h2>
    <p class="text-zinc-400 mb-8">{description || '브리츠메디 전문 컨설턴트가 클리닉에 최적화된 솔루션을 안내해드립니다.'}</p>
    <div class="flex justify-center gap-3">
      <a href="/contact" class="px-8 py-3.5 bg-teal-600 text-white text-sm font-medium rounded-lg hover:bg-teal-500 transition-colors">
        무료 상담 신청
      </a>
      <a href="/certifications" class="px-8 py-3.5 border border-zinc-700 text-zinc-300 text-sm font-medium rounded-lg hover:border-zinc-500 hover:text-white transition-colors">
        인증 현황 보기
      </a>
    </div>
  </div>
</section>
