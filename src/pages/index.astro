---
import BaseLayout from '@layouts/BaseLayout.astro';
import SchemaOrg from '@components/aeo/SchemaOrg.astro';
import CTASection from '@components/common/CTASection.astro';
import UpdateDate from '@components/common/UpdateDate.astro';
import { organizationSchema } from '@lib/schema';
import { getAllPosts } from '@data/blog';

const latestPosts = getAllPosts().slice(0, 3);
---

<BaseLayout>
  <SchemaOrg schema={organizationSchema()} slot="head" />

  <!-- Hero 섹션 -->
  <section class="relative overflow-hidden bg-gradient-to-br from-slate-900 via-slate-800 to-blue-900 px-4 sm:px-6 lg:px-8">
    <div class="absolute inset-0 opacity-30">
      <div class="absolute left-1/4 top-1/4 h-96 w-96 rounded-full bg-blue-500/20 blur-3xl"></div>
      <div class="absolute right-1/4 bottom-1/4 h-64 w-64 rounded-full bg-blue-400/10 blur-3xl"></div>
    </div>
    <div class="relative mx-auto flex min-h-[85vh] max-w-7xl items-center md:min-h-[90vh]">
      <div class="grid items-center gap-12 py-20 md:grid-cols-2 md:py-28">
        <div>
          <h1 class="mb-6 text-4xl font-bold leading-tight tracking-tight text-white md:text-5xl lg:text-6xl">
            토로이달 RF 기술로<br />피부 미용의 새로운 기준을<br />만듭니다
          </h1>
          <p class="mb-10 max-w-2xl text-lg text-slate-300 md:text-xl">
            브리츠메디(BRITZMEDI)는 토로이달 코일 기반 Multi-wave RF 기술로 피부 미용 의료기기를 개발·제조하는 한국 기업입니다.
          </p>
          <div class="flex flex-col gap-4 sm:flex-row">
            <a href="/contact" class="rounded-xl bg-blue-600 px-8 py-4 text-center font-semibold text-white transition-all duration-300 hover:bg-blue-700 hover:shadow-lg">
              상담 신청
            </a>
            <a href="/products" class="rounded-xl border-2 border-white/30 px-8 py-4 text-center font-semibold text-white transition-all duration-300 hover:bg-white/10">
              제품 알아보기
            </a>
          </div>
        </div>
        <div class="hidden items-center justify-center md:flex">
          <div class="flex h-80 w-80 items-center justify-center rounded-3xl border border-white/10 bg-white/5 backdrop-blur-sm">
            <div class="text-center">
              <p class="text-6xl font-bold text-white/80">RF</p>
              <p class="mt-2 text-sm text-slate-400">Toroidal Coil Technology</p>
            </div>
          </div>
        </div>
      </div>
    </div>
    <!-- 신뢰 배지 바 -->
    <div class="relative mx-auto max-w-7xl pb-12">
      <div class="flex flex-wrap items-center justify-center gap-6 rounded-2xl bg-white/5 px-6 py-4 backdrop-blur-sm md:gap-10">
        <span class="text-sm font-medium text-slate-300">FDA 510(k)</span>
        <span class="text-slate-600">&middot;</span>
        <span class="text-sm font-medium text-slate-300">CE Mark</span>
        <span class="text-slate-600">&middot;</span>
        <span class="text-sm font-medium text-slate-300">ISO 13485</span>
        <span class="text-slate-600">&middot;</span>
        <span class="text-sm font-medium text-slate-300">40개국+ 파트너</span>
      </div>
    </div>
  </section>

  <!-- 제품 쇼케이스 -->
  <section class="reveal px-4 py-20 sm:px-6 lg:px-8 md:py-28">
    <div class="mx-auto max-w-7xl">
      <div class="mb-12 text-center">
        <h2 class="mb-3 text-3xl font-bold tracking-tight text-slate-900 md:text-4xl">제품 라인업</h2>
        <p class="text-slate-500">세계가 인정한 RF 기술</p>
      </div>
      <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
        {[
          { name: 'TORR RF', desc: 'Multi-wave RF 피부 미용 의료기기', href: '/products/torr-rf', initial: 'T' },
          { name: 'ULBLANC', desc: '프리미엄 RF 스킨케어 디바이스', href: '/products/ulblanc', initial: 'U' },
          { name: 'NEWCHAE', desc: '차세대 RF 피부 관리 솔루션', href: '/products/newchae', initial: 'N' },
          { name: 'LUMINO WAVE', desc: 'LED + RF 복합 디바이스', href: '/products/lumino-wave', initial: 'L' },
        ].map((product) => (
          <a href={product.href} class="group rounded-2xl border border-slate-100 bg-white p-6 transition-all duration-300 hover:-translate-y-1 hover:shadow-lg">
            <div class="mb-4 flex h-48 items-center justify-center rounded-xl bg-slate-50">
              <span class="text-5xl font-bold text-slate-200 transition-colors group-hover:text-blue-200">{product.initial}</span>
            </div>
            <h3 class="mb-1 text-lg font-bold text-slate-900">{product.name}</h3>
            <p class="mb-3 text-sm text-slate-500">{product.desc}</p>
            <span class="text-sm font-semibold text-blue-700">자세히 보기 &rarr;</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- 왜 브리츠메디인가 -->
  <section class="reveal bg-slate-50 px-4 py-20 sm:px-6 lg:px-8 md:py-28">
    <div class="mx-auto max-w-7xl">
      <div class="mb-12 text-center">
        <h2 class="mb-3 text-3xl font-bold tracking-tight text-slate-900 md:text-4xl">왜 브리츠메디인가</h2>
      </div>
      <div class="grid gap-8 md:grid-cols-3">
        {[
          { icon: '\u{1F52C}', title: '독자적 토로이달 RF 기술', desc: '기존 모노폴라/바이폴라 방식 대비 균일한 에너지 전달과 깊은 침투 깊이를 구현하는 특허 기술입니다.' },
          { icon: '\u{1F30D}', title: '40개국+ 글로벌 파트너 네트워크', desc: '전 세계 40개국 이상의 파트너 네트워크를 통해 글로벌 시장에서 입증된 브랜드입니다.' },
          { icon: '\u2705', title: 'FDA/CE/ISO 트리플 인증', desc: 'FDA 510(k), CE Mark, ISO 13485 등 주요 글로벌 의료기기 인증을 획득하여 품질과 안전성을 보증합니다.' },
        ].map((item) => (
          <div class="rounded-2xl border border-slate-100 bg-white p-8 shadow-sm transition-all duration-300 hover:shadow-md">
            <div class="mb-4 flex h-14 w-14 items-center justify-center rounded-xl bg-blue-50 text-2xl">
              {item.icon}
            </div>
            <h3 class="mb-3 text-lg font-bold text-slate-900">{item.title}</h3>
            <p class="text-sm leading-relaxed text-slate-600">{item.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 숫자 섹션 -->
  <section class="reveal bg-gradient-to-r from-slate-900 to-blue-900 px-4 py-16 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-7xl">
      <div class="grid grid-cols-2 gap-8 md:grid-cols-4">
        {[
          { number: '3+', label: '글로벌 인증 (FDA/CE/ISO)' },
          { number: '10+', label: '특허 보유' },
          { number: '40+', label: '파트너 국가' },
          { number: '100K+', label: '누적 시술' },
        ].map((stat) => (
          <div class="text-center">
            <p class="text-4xl font-bold text-white md:text-5xl">{stat.number}</p>
            <div class="mx-auto my-3 w-12 border-b-2 border-amber-400"></div>
            <p class="text-sm text-slate-300">{stat.label}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="bg-blue-700 px-4 py-16 text-center text-white sm:px-6 lg:px-8">
    <div class="mx-auto max-w-3xl">
      <h2 class="mb-4 text-2xl font-bold md:text-3xl">브리츠메디와 함께 시작하세요</h2>
      <p class="mb-8 text-blue-100">토로이달 RF 기술의 차이를 직접 경험해 보세요. 무료 상담을 신청하실 수 있습니다.</p>
      <div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
        <a href="/contact" class="rounded-xl bg-white px-8 py-4 font-semibold text-blue-700 transition-all duration-300 hover:bg-blue-50 hover:shadow-lg">
          무료 상담 신청
        </a>
        <a href="/products" class="rounded-xl border-2 border-white/30 px-8 py-4 font-semibold transition-all duration-300 hover:bg-white/10">
          카탈로그 보기
        </a>
      </div>
    </div>
  </section>

  <!-- 최신 소식 -->
  <section class="reveal px-4 py-20 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-7xl">
      <div class="mb-12 text-center">
        <h2 class="mb-3 text-3xl font-bold tracking-tight text-slate-900 md:text-4xl">최신 소식</h2>
      </div>
      <div class="grid gap-6 md:grid-cols-3">
        {latestPosts.map((post) => (
          <a href={`/blog/${post.slug}`} class="group overflow-hidden rounded-2xl border border-slate-100 bg-white transition-all duration-300 hover:shadow-lg">
            <div class="flex h-48 items-center justify-center bg-slate-100">
              <span class="text-3xl text-slate-300">{'\u{1F4DD}'}</span>
            </div>
            <div class="p-6">
              <span class="inline-block rounded-full bg-blue-100 px-3 py-1 text-xs font-medium text-blue-700">{post.categoryLabel}</span>
              <h3 class="mt-3 font-bold text-slate-900 transition-colors group-hover:text-blue-700">{post.title}</h3>
              <p class="mt-2 text-sm text-slate-500">{post.publishedAt}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <UpdateDate />
  </div>
</BaseLayout>
